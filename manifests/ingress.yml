apiVersion: extensions/v1beta1
kind: Ingress
metadata:
  name: heroes-node-backend
  annotations:
    kubernetes.io/ingress.class: azure/application-gateway
spec:
  rules:
  - http:
      paths:
      - path: /
        backend:
          serviceName: heroes-node-backend
          servicePort: 80